version: '3.8'
services:
  dev:
    build: .
    image: imit-pg
    volumes:
      - ~/.ssh:/root/.ssh
      - ~/.git:/root/.git
      - .:/workspaces:cached
      - .:/com.docker.devenvironments.code

 
